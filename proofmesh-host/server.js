const http=require('http');const fs=require('fs');const path=require('path');const port=process.env.PORT||3000;const file=path.join(__dirname,'index.html');http.createServer((req,res)=>{if(req.url!=='/'&&req.url!=='/index.html'){res.writeHead(302,{Location:'/'});return res.end()}fs.readFile(file,(e,d)=>{if(e){res.writeHead(500);return res.end('ProofMesh failed to load')}res.writeHead(200,{'Content-Type':'text/html; charset=utf-8','Cache-Control':'no-store'});res.end(d)})}).listen(port,'0.0.0.0',()=>console.log('ProofMesh listening on '+port));